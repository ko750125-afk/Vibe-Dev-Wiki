# Vibe Dev-Wiki 회귀 테스트 시나리오

목적: 기존 시스템을 유지한 채 기능 확장 시, 핵심 플로우(로그인/권한/CRUD/검색/테마) 회귀를 빠르게 검증한다.

## 0) 공통 사전 조건

- 개발 서버 실행: `npm run dev`
- DB 스키마 적용: `supabase_schema.sql` 실행 완료
- 테스트 계정 준비:
  - 관리자 계정 이메일 1개 ( `vibe_admin_users` 에 등록 )
  - 일반 계정 이메일 1개 ( `vibe_admin_users` 미등록 )
- 브라우저 캐시 이슈 방지를 위해 시나리오 시작 전 새 시크릿 창 사용 권장

---

## 1) 로그인 회귀 시나리오

### TC-LOGIN-001: Google 로그인 성공
- 목적: 기본 인증 흐름 정상 동작 확인
- 절차:
  1. `/login` 접근
  2. `Google로 계속하기` 클릭
  3. Google 인증 완료
- 기대 결과:
  - `/auth/callback` 이후 `/`로 이동
  - 세션이 생성되어 메인 화면 렌더링

### TC-LOGIN-002: 인증 실패 처리
- 목적: 실패 시 안전한 폴백 확인
- 절차:
  1. OAuth 도중 취소 또는 실패 유도
  2. 콜백 처리 결과 확인
- 기대 결과:
  - `/login?error=auth_failed` 로 이동
  - 앱이 크래시 없이 로그인 화면 유지

### TC-LOGIN-003: 로그아웃 동작
- 목적: 세션 종료 검증
- 절차:
  1. 로그인 상태에서 사이드바 `SIGN OUT` 클릭
- 기대 결과:
  - 세션 해제
  - 보호 페이지 재진입 시 `/login` 리다이렉트

---

## 2) 권한 회귀 시나리오

### TC-AUTHZ-001: 관리자 권한 인식
- 목적: `vibe_admin_users` 기반 권한 모델 검증
- 절차:
  1. 관리자 계정으로 로그인
  2. 사이드바/카드에서 관리자 기능 확인
- 기대 결과:
  - 관리자 전용 UI 노출 (추가/수정/삭제 진입 가능)
  - 쓰기 액션 성공

### TC-AUTHZ-002: 일반 사용자 제한
- 목적: 비관리자 쓰기 제한 검증
- 절차:
  1. 일반 계정으로 로그인
  2. 관리자 기능 노출 여부 확인
  3. 직접 액션 호출이 가능한 경우(개발자 도구 등) 쓰기 시도
- 기대 결과:
  - 관리자 UI 비노출
  - 쓰기 액션 거부(Forbidden 또는 RLS 차단)
  - 읽기 기능은 정상 동작

### TC-AUTHZ-003: 관리자 테이블 미존재 폴백
- 목적: 초기 환경 안정성 확인
- 절차:
  1. `vibe_admin_users` 미생성 상태(또는 접근 불가 상태)에서 로그인
- 기대 결과:
  - 앱 크래시 없이 동작
  - `isAdmin=false`로 안전 폴백

---

## 3) CRUD 회귀 시나리오 (관리자 계정 기준)

### TC-CRUD-SEC-001: 섹터 생성
- 목적: 섹터 추가 흐름 검증
- 절차:
  1. 카테고리 선택
  2. 섹터 추가 버튼 클릭 후 이름 입력/확정
- 기대 결과:
  - 새 섹터가 목록에 표시
  - 페이지 새로고침 후에도 유지

### TC-CRUD-SEC-002: 섹터 수정
- 목적: 섹터 이름 변경 검증
- 절차:
  1. 기존 섹터 편집 진입
  2. 이름 변경 후 저장
- 기대 결과:
  - 변경 이름 즉시 반영
  - 새로고침 후 유지

### TC-CRUD-SEC-003: 섹터 삭제
- 목적: 삭제 확인/처리 검증
- 절차:
  1. 섹터 삭제 버튼 클릭
  2. 확인 다이얼로그 승인
- 기대 결과:
  - 섹터가 목록에서 제거
  - 관련 노트 표시가 일관되게 갱신

### TC-CRUD-NOTE-001: 노트 생성
- 목적: 노트 생성 및 목록 반영 검증
- 절차:
  1. 관리자 컨트롤에서 노트 생성 모달 진입
  2. `title/content/stage/block_type` 입력 후 저장
- 기대 결과:
  - 카드 목록에 신규 노트 표시
  - 새로고침 후 유지

### TC-CRUD-NOTE-002: 노트 수정
- 목적: 노트 편집 반영 검증
- 절차:
  1. 노트 상세/편집 진입
  2. 내용 수정 후 저장
- 기대 결과:
  - 카드/상세 내용에 수정 반영
  - 검색 결과에도 최신 내용 반영

### TC-CRUD-NOTE-003: 노트 삭제
- 목적: 노트 삭제 안정성 검증
- 절차:
  1. 노트 삭제 실행
- 기대 결과:
  - 목록에서 즉시 제거
  - 새로고침 후 재등장하지 않음

---

## 4) 검색 회귀 시나리오

### TC-SEARCH-001: 제목 검색
- 절차: 고유 키워드로 검색
- 기대 결과: 제목 매칭 노트만 노출

### TC-SEARCH-002: 본문 검색
- 절차: 본문에만 존재하는 키워드로 검색
- 기대 결과: 본문 기준 검색 결과 노출

### TC-SEARCH-003: 단계명 검색
- 절차: `Step 1` 등 단계명 검색
- 기대 결과: 단계명 포함 노트 노출

### TC-SEARCH-004: 검색/필터 상호작용
- 절차:
  1. Stage 필터를 특정 값으로 설정
  2. 검색어 입력
- 기대 결과:
  - 검색 모드에서는 전체 검색 정책이 유지되고, 안내 UI가 정책과 일치

### TC-SEARCH-005: 검색 리셋
- 절차: 검색어 입력 후 클리어 버튼 클릭
- 기대 결과:
  - 검색어 초기화
  - 기본 목록 복귀

---

## 5) 테마 회귀 시나리오

### TC-THEME-001: 라이트/다크 전환
- 목적: 토글 동작 검증
- 절차:
  1. 사이드바 테마 토글 클릭
  2. 라이트/다크 반복 전환
- 기대 결과:
  - 즉시 테마 반영
  - 주요 컴포넌트(사이드바/카드/모달) 대비 및 가독성 유지

### TC-THEME-002: 라우트 이동 후 테마 유지
- 절차:
  1. 테마 변경
  2. 섹터 이동/모달 열기/닫기 수행
- 기대 결과:
  - 테마가 유지되고 깜빡임/깨짐 없음

### TC-THEME-003: 새로고침 후 테마 유지
- 절차: 테마 변경 후 브라우저 새로고침
- 기대 결과:
  - 이전 테마 상태 유지

---

## 6) 스모크(릴리즈 전 최소 검증) 체크리스트

- [ ] 관리자 로그인 성공 / 로그아웃 성공
- [ ] 일반 사용자 로그인 시 관리자 UI 비노출
- [ ] 섹터 생성/수정/삭제 각 1회 성공
- [ ] 노트 생성/수정/삭제 각 1회 성공
- [ ] 검색(제목/본문) 정상 동작
- [ ] Stage 필터 선택 반영
- [ ] 테마 라이트/다크 전환 및 유지 확인
- [ ] 콘솔 치명 오류 없음

---

## 7) 장애 기록 템플릿

- 테스트 케이스 ID:
- 실행 환경(브라우저/OS):
- 재현 절차:
- 실제 결과:
- 기대 결과:
- 콘솔/네트워크 로그:
- 스크린샷:
- 심각도(High/Medium/Low):

